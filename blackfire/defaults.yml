# vim: ft=yaml
# Defaults for blackfire-formula

blackfire:
  enabled: true
  pkgs:
    - blackfire-agent
    - blackfire-php
  server_id: 'replace'
  server_token: 'replace'
  log_level: 1
  agent_user: blackfire
